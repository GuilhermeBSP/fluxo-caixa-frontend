version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "window.API_BASE = '${API_BASE_URL}';" > config.js
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
